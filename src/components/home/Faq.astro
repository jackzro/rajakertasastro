---
import { getFaq } from "@/helpers/rajaKertas";
import { Button } from "../ui/button";
import FaqCom from '../Faq'
const response = await getFaq()


---

<section class="bg-white text-black py-20 px-4 grid grid-cols-1 lg:grid-cols-3 lg:space-x-20 space-y-2 lg:px-10">
<div class="flex space-y-6 flex-col col-span-1 mb-10">
    <h1 class="text-6xl font-bold">{response.value.title}</h1>
    <p>
      Temukan jawaban atas pertanyaan yang sering diajukan tentang produk
      dan layanan kami.
    </p>

    <Button className="w-1/2">Hubungi Kami</Button>

  </div>
  <div class="col-span-2">
   <FaqCom client:load list={response.value.list}/>
  </div>
</section>